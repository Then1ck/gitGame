package camera;

import java.awt.Graphics2D;

import entities.Player;

public class Camera {
	private Player p1;
	private int offsetX, offsetY;
	private int DistToLeft=640, DistToTop=360;
	
	public Camera(Player P1) {
		p1 = P1;
		offsetX = DistToLeft;
		offsetY = DistToTop;
	}
	
	public void camChase() {
		offsetX = -(offsetX - (offsetX - p1.getLocX())/2) + DistToLeft;
		offsetY = -(offsetY - (offsetY - p1.getLocY())/2) + DistToTop;
	}
	
	public void  setCam(Graphics2D g2) {
		g2.translate(offsetX, offsetY);
	}
}
